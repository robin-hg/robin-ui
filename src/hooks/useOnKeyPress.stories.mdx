import { Meta } from '@storybook/addon-docs'

<Meta title="Hooks/useOnKeyPress" />

# useOnKeyPress

Invokes the function `fn` when `key` is pressed.

`const useOnKeyPress: (key: string, fn: () => void, disabled?: boolean) => void`
