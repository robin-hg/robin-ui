import { Meta } from '@storybook/addon-docs'

<Meta title="Hooks/useCombinedRef" />

# useCombinedRef

Combines multiple refs to a single ref.

`const useCombinedRef: <T extends any>(...refs: React.Ref<T>[] | null | undefined>) => React.Ref<T>`
