import { Meta } from '@storybook/addon-docs'

<Meta title="Hooks/useOnScreen" />

# useOnScreen

Returns a boolean depending on whether if the `target` is on screen or not (+/- `rootMargin`px).

`const useOnScreen: (refEl: React.RefObject<Element>, rootMargin?: string) => boolean`
