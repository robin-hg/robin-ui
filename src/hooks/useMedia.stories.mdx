import { Meta } from '@storybook/addon-docs'

<Meta title="Hooks/useMedia" />

# useMedia

Simulates a css media query

`const useMedia: (breakpoint: keyof RUITheme['breakpoints'] | number, direction?: 'up' | 'down') => boolean`
