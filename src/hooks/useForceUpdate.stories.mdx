import { Meta } from '@storybook/addon-docs'

<Meta title="Hooks/useForceUpdate" />

# useForceUpdate

Returns a function that force updates the component when called.

`const useForceUpdate: () => () => void`
