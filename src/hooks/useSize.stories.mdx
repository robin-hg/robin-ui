import { Meta } from '@storybook/addon-docs'

<Meta title="Hooks/useSize" />

# useSize

Returns the size of `target` using `getBoundingClientRect`.

`const useSize: (refEl: React.RefObject<Element>, dependencies?: any[]) => DOMRect | undefined`
