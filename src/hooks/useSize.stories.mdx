import { Meta } from '@storybook/addon-docs'

<Meta title="Hooks/useSize" />

# useSize

Returns the size of `target` using `getBoundingClientRect`.

`const useSize: (refEl: React.RefObject<HTMLElement>, dependencies?: any[]) => DOMRect | undefined`
