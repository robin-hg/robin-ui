import { Meta } from '@storybook/addon-docs'

<Meta title="Hooks/useMediaDevice" />

# useMediaDevice

Returns whether if the current screen size is for the given device type(s)

`const useMediaDevice: (type: DeviceType | DeviceType[]) => boolean`
