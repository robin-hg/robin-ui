import { Meta } from '@storybook/addon-docs'

<Meta title="Hooks/useLockWindowScroll" />

# useLockWindowScroll

Prevents the body from scrolling using css.

`const useLockWindowScroll: (lock: boolean) => void`
