import { Meta } from '@storybook/addon-docs'

<Meta title="Hooks/useTimeoutEffect" />

# useTimeoutEffect

A shorthand custom `useEvent` hook that automatically sets/clears timeouts. Defaults to 500ms.

`const useTimeoutEffect: (callback: () => void, timeout?: number, dependencies?: any[]) => void`
