import { Meta } from '@storybook/addon-docs'

<Meta title="Hooks/useDebounce" />

# useDebounce

Returns a debounced `value` that updates until after `timeout` miliseconds have elapsed since last update. Defaults to 500ms.

`const useDebounce: <T extends any>(value: T, timeout?: number) => T`
