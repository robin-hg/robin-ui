import { Meta } from '@storybook/addon-docs'

<Meta title="Hooks/useKeyPress" />

# useKeyPress

Invokes the function `fn` when `key` is pressed.

`const useKeyPress: (key: string, fn: () => void, disabled?: boolean) => void`
