import { Meta } from '@storybook/addon-docs'

<Meta title="Hooks/usePrevious" />

# usePrevious

Returns the value of `value` before the component was updated.

`const usePrevious: <T extends any>(value: T) => T | undefined`
